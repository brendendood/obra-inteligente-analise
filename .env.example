# ===================================================================
# 🔧 ARQUIVO DE EXEMPLO - VARIÁVEIS DE AMBIENTE
# ===================================================================
# 
# Este arquivo contém exemplos de todas as variáveis de ambiente
# utilizadas no projeto MadenAI. Copie para .env e ajuste os valores.
#
# IMPORTANTE: 
# - NUNCA commite o arquivo .env real com credenciais
# - Use apenas valores de exemplo/placeholders neste arquivo
# - Documentação completa em: docs/env/VARS.md
#
# ===================================================================

# ===================================================================
# 🗄️ SUPABASE (OBRIGATÓRIO)
# ===================================================================
# Configuração do backend principal - Supabase
# Obtenha estes valores no Dashboard do Supabase > Settings > API

VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.your-anon-key-here
VITE_SUPABASE_PROJECT_ID=your-project-id

# ===================================================================
# ⚡ VITE (OPCIONAL)
# ===================================================================
# Configurações do servidor de desenvolvimento

# Porta do servidor de desenvolvimento (padrão: 8080)
VITE_DEV_PORT=8080

# Host do servidor (padrão: :: para todos os IPs)
VITE_HOST=::

# Porta do preview (padrão: 4173)
VITE_PREVIEW_PORT=4173

# ===================================================================
# 🔍 DEBUG E DESENVOLVIMENTO (OPCIONAL)
# ===================================================================
# Configurações para debug e desenvolvimento

# Habilitar modo debug (padrão: false em produção)
VITE_DEBUG_MODE=false

# Habilitar sourcemaps (padrão: true em dev, false em prod)
VITE_SOURCEMAP=true

# ===================================================================
# 🎨 FRONTEND (OPCIONAL)
# ===================================================================
# Configurações da aplicação frontend

# URL base da aplicação (padrão: /)
VITE_BASE_URL=/

# Caminho público para assets (padrão: /)
VITE_PUBLIC_PATH=/

# ===================================================================
# 📊 ANALYTICS E MONITORAMENTO (OPCIONAL)
# ===================================================================
# Ferramentas de analytics e monitoramento (não implementadas)

# Google Analytics ID (não configurado)
# VITE_ANALYTICS_ID=G-XXXXXXXXXX

# Sentry DSN (não configurado)
# VITE_SENTRY_DSN=https://your-dsn@sentry.io/project-id

# ===================================================================
# 🔒 SEGURANÇA (OPCIONAL)
# ===================================================================
# Configurações de segurança

# Habilitar Content Security Policy (padrão: false)
VITE_ENABLE_CSP=false

# Usar cookies seguros (padrão: true em produção)
VITE_SECURE_COOKIES=true

# Desabilitar React DevTools em produção (padrão: false)
VITE_DISABLE_DEVTOOLS=false

# ===================================================================
# 🏗️ BUILD E OTIMIZAÇÃO (OPCIONAL)
# ===================================================================
# Configurações de build e otimização

# Target de build (padrão: esnext)
VITE_BUILD_TARGET=esnext

# Limite de aviso de chunk size em KB (padrão: 800)
VITE_CHUNK_SIZE_WARNING=800

# ===================================================================
# 🌐 CONFIGURAÇÕES ESPECÍFICAS DE AMBIENTE
# ===================================================================

# Para DESENVOLVIMENTO LOCAL:
# NODE_ENV=development
# VITE_DEBUG_MODE=true
# VITE_SOURCEMAP=true

# Para STAGING/PREVIEW:
# NODE_ENV=production
# VITE_DEBUG_MODE=true
# VITE_SOURCEMAP=false

# Para PRODUÇÃO:
# NODE_ENV=production
# VITE_DEBUG_MODE=false
# VITE_SOURCEMAP=false
# VITE_SECURE_COOKIES=true

# ===================================================================
# ❌ VARIÁVEIS NÃO UTILIZADAS
# ===================================================================
# Estas variáveis são comuns em outros projetos mas NÃO são usadas:
#
# REACT_APP_*          (projeto usa Vite, não Create React App)
# NEXT_PUBLIC_*        (projeto não é Next.js)
# VUE_APP_*            (projeto não é Vue)
# DATABASE_URL         (usa Supabase, não conexão direta)
# REDIS_URL            (não usa Redis)
# SESSION_SECRET       (usa Supabase Auth)
# JWT_SECRET           (gerenciado pelo Supabase)

# ===================================================================
# 📝 NOTAS IMPORTANTES
# ===================================================================
#
# 1. APENAS 3 VARIÁVEIS SÃO OBRIGATÓRIAS:
#    - VITE_SUPABASE_URL
#    - VITE_SUPABASE_PUBLISHABLE_KEY  
#    - VITE_SUPABASE_PROJECT_ID
#
# 2. CHAVES PÚBLICAS:
#    As chaves Supabase são PÚBLICAS e seguras para exposição
#    no frontend. Não contêm informações sensíveis.
#
# 3. SECRETS PRIVADOS:
#    Secrets sensíveis são gerenciados via Supabase Secrets
#    e acessíveis apenas em Edge Functions.
#
# 4. N8N WEBHOOKS:
#    URLs do N8N são hardcoded no código por serem públicos.
#    Não requerem configuração via variáveis de ambiente.
#
# 5. APIS EXTERNAS:
#    APIs como geolocalização são acessadas via Edge Functions
#    para manter as chaves seguras.
#
# ===================================================================

# 🎯 PRÓXIMOS PASSOS:
# 1. Copie este arquivo: cp .env.example .env
# 2. Configure as 3 variáveis obrigatórias do Supabase
# 3. Execute: npm run dev
# 4. Acesse: http://localhost:8080